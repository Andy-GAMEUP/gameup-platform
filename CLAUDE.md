# GameUp Platform - Claude 작업 지침

## 커밋 규칙

- **커밋은 사용자가 명시적으로 "커밋해"라고 할 때만 한다**
- 코드 작업이 끝나도 자동으로 커밋하지 않는다
- 커밋 브랜치는 항상 `dev-capcloud`만
- `main` 머지는 사용자가 "머지해", "main에 올려" 등 명시적으로 지시할 때만 진행
- 빌드 검증 후에도 자동 커밋/자동 main push 금지

### 커밋 전 필수 절차
커밋 요청을 받으면 **실제 커밋 전에** 아래 형식으로 변경 내역을 먼저 대화창에 출력한다:

| 파일 | 작업 내용 |
|------|-----------|
| `경로/파일명` | 변경 내용 요약 |

사용자 확인 없이 바로 커밋하지 않는다. 단, 사용자가 "바로 커밋해" 등 확인 생략을 명시하면 바로 진행해도 된다.

## 터미널 2개 동시 작업 충돌 규칙

- 사용자는 같은 저장소(`~/projects/gameup-platform`)에서 터미널 2개("Claude Code - GameUp.bat", "Claude Code - GameUp_2.bat")를 동시에 열어 각각 별도 세션으로 작업할 수 있다
- 작업 중 다음 상황을 발견하면 **즉시 현재 작업을 중단**하고 **터미널에 충돌 상황을 알린다**:
  - 같은 파일을 다른 터미널(세션)에서도 동시에 수정 중이라는 것을 알게 된 경우
  - 세션 시작 이후 내가 건드리지 않은 파일이 예상치 못하게 바뀐 것을 발견한 경우 (`git status`/`git diff` 등에서 확인)
- 알림만 하고 멈춘다 — git stash, git checkout 등으로 되돌리거나 임의로 병합/정리를 시도하지 않는다
- 이후 진행 여부는 사용자의 지시를 받은 뒤 결정한다

## 머지 작업 규칙

### 머지 절차 (순서대로 실행)
1. `main`으로 `git checkout` 하지 않는다
2. `origin/main`을 현재 브랜치에 머지한다 (`git merge origin/main`)
3. 충돌 발생 시 충돌 파일만 수정한다
4. `npx tsc --noEmit -p apps/web/tsconfig.json`으로 빌드 검증한다
5. 검증 통과 후 `git push origin dev-capcloud:main`으로 push한다
6. GitHub Actions 배포 결과를 확인한다 (아래 명령어 사용)
7. 배포 성공 시 사용자에게 완료 알림을 한다

### 배포 확인 명령어
```bash
TOKEN=$(git remote get-url origin | grep -oP 'ghp_[^@]+')
curl -s -H "Authorization: token $TOKEN" \
  "https://api.github.com/repos/Andy-GAMEUP/gameup-platform/actions/runs?per_page=3" \
  | python3 -c "
import sys, json
data = json.load(sys.stdin)
for r in data.get('workflow_runs', []):
    print(f\"{r['status']} | {r['conclusion'] or '진행중'} | {r['created_at'][:19]} | {r['head_commit']['message'][:50]}\")
"
```
- `completed / success` → 배포 완료
- `in_progress` → 배포 진행 중 (잠시 후 재확인)
- `completed / failure` → 배포 실패 (사용자에게 알림)

### 충돌 파일 외 작업 금지
- 머지 작업 중 리팩토링, 기능 추가, 불필요한 파일 수정 금지
- 추가 작업이 필요하면 사용자에게 알리고 허락을 받은 후 진행

## 플랫폼 운영 규칙 문서

- **플랫폼 비즈니스 규칙은 반드시 `docs/platform-rules.md`를 먼저 확인한다**
- 규칙이 변경되거나 새로운 프로세스가 추가되면 **작업 완료 후 `docs/platform-rules.md`를 업데이트한다**
- 규칙 변경 예시: 회원 가입/승인 흐름, 파트너 등록 절차, 권한 체계, 상태값 변경 등

## 화면 구성 동일 적용 기본 원칙

- 화면 구성(레이아웃, 섹션 배치, UI 요소 등)을 수정하면, 사용자가 "이 화면만 따로 할게" 등으로 특정 화면을 명시적으로 예외 지정하지 않는 한 **묻지 않고 같은 종류/그룹의 나머지 화면 전부에도 동일하게 적용**한다
- "다른 화면에도 적용할까요?"라고 되묻지 않는다
- 아래에 항목별 세부 규칙 문서(배지/별점/날짜/카드/라벨 등)가 있으면 그 문서를 따르고, 별도 문서가 없는 화면 구성 변경도 이 기본 원칙을 따른다
- 단, 사용자가 화면/유형별로 명시적으로 다르게 구성해달라고 요청한 경우(예: 회원 상세정보 페이지의 기업회원/게임회원/관리자 레이아웃 분리)는 예외이며, 그 예외 지정은 유지한다

## 게시물 작성용 입력 필드 동일 적용 규칙

- **`docs/content-editor-consistency-rule.md`를 반드시 확인한다**
- 이 문서에 나열된 "동일 그룹" 입력 필드(리치 텍스트 에디터) 중 하나를 수정/기능 추가하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 나머지 전부에도 동일하게 적용**한다
- 새로운 게시물류 입력 필드를 추가하면 이 문서의 목록도 함께 업데이트한다

## 확인 팝업 통일 규칙

- **`docs/confirm-modal-consistency-rule.md`를 반드시 확인한다**
- 삭제/종료/복원/승인 등 사용자 확인이 필요한 모든 동작은 브라우저 기본 `window.confirm()`이 아니라 `apps/web/src/components/ConfirmModal.tsx`로 통일한다
- 관리자/개발자/유저 포털 구분 없이 전부 동일하게 적용하며, 새로 확인 팝업을 추가할 때도 `window.confirm()`을 쓰지 않는다

## 알림(alert) 팝업 통일 규칙

- **`docs/alert-modal-consistency-rule.md`를 반드시 확인한다**
- "확인" 버튼 하나만 있는 단순 알림(저장 완료, 처리 실패 등)은 브라우저 기본 `alert()`이 아니라 `apps/web/src/components/AlertModal.tsx`로 통일한다
- `AlertModal`은 공용 컴포넌트라 하나를 수정하면 묻지 않고 이 컴포넌트를 쓰는 나머지 화면 전부에 자동 반영된다
- 기존 코드에 남은 `alert()`을 전체 일괄 교체하지는 않는다 — 사용자가 특정 화면을 지목해 바꿔달라고 할 때마다 그 화면의 `alert()`을 `AlertModal`로 교체하고, 새로 추가하는 알림도 항상 `AlertModal`을 쓴다

## 프로젝트 카드 레이아웃 동일 적용 규칙

- **`docs/project-card-consistency-rule.md`를 반드시 확인한다**
- 이 문서에 나열된 "동일 그룹"(프로젝트를 카드로 보여주는 화면들) 중 하나의 카드 레이아웃을 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 나머지 전부에도 동일하게 적용**한다
- 새로 프로젝트 카드를 보여주는 화면을 추가하면 이 문서의 목록도 함께 업데이트한다

## 좌우 스크롤 화살표 동일 적용 규칙

- **`docs/scroll-arrow-consistency-rule.md`를 반드시 확인한다**
- 가로로 나열된 카드/배너를 좌우 화살표로 넘기는 UI(색상/크기/위치/아이콘) 중 하나를 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 이 문서에 나열된 나머지 전부에도 동일하게 적용**한다
- 기준은 메인페이지 히어로 배너 캐러셀/추천 게임 대형 배너의 화살표(반투명 검정 원형, 흰색 아이콘, 마우스 오버 시에만 노출)
- 새로 좌우 화살표 스크롤 UI를 추가하면 이 문서의 목록도 함께 업데이트한다

## 게임 카드 레이아웃 동일 적용 규칙

- **`docs/game-card-consistency-rule.md`를 반드시 확인한다**
- 이 문서에 나열된 "동일 그룹"(베타존/라이브게임 등 게임을 카드로 보여주는 화면들) 중 하나의 카드 레이아웃을 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 나머지 전부에도 동일하게 적용**한다
- 새로 게임 카드를 보여주는 화면을 추가하면 이 문서의 목록도 함께 업데이트한다

## 이미지 업로드 필수 크기 ↔ 실제 노출 크기 동기화 규칙

- **`docs/image-upload-size-consistency-rule.md`를 반드시 확인한다**
- 게임 아이콘/히어로 배너/스크린샷, 관리자 배너 이미지는 업로드 시 강제하는 "필수 크기(px)"와 실제 사이트에 노출되는 화면의 CSS 크기/비율이 항상 서로 맞아야 한다
- 노출 화면(디자인) 쪽을 조정하면 업로드 필수 크기도, 업로드 필수 크기를 조정하면 노출 화면의 CSS도 — 사용자가 예외를 지정하지 않는 한 **묻지 않고 양쪽을 함께 수정**한다
- 단, 프로필 사진(아바타) 업로드는 이 규칙 대상이 아니다 — 자유로운 크기로 업로드 가능하도록 유지한다
- **이미지 슬롯마다 그 슬롯 전용 원본만 쓴다** — 하나의 이미지를 조정(리사이즈/크롭)해서 다른 슬롯에도 재사용하지 않는다. 사용자가 그렇게 요청하면 바로 진행하지 말고 **이 규칙이 있다는 것을 터미널에 먼저 알린다**
- **이미지 비율(가로:세로)은 절대 변경하지 않는다.** 두 가지 다 포함: ① 업로드 요구 크기(px) 자체를 조정할 때 원본 비율을 유지한다, ② **화면에 노출되는 박스(컨테이너)의 비율도 그 이미지의 원본/업로드 비율과 맞춰야 한다** — 레이아웃(높이/폭)을 바꾸다가 실수로 노출 박스 비율이 달라지면(예: 정사각형 아이콘을 가로로 넓은 박스에 넣게 됨) `object-cover`라 찌그러지진 않아도 원치 않게 과하게 크롭되므로, 다른 요소(높이 등)를 조정할 때도 이미지가 들어가는 박스는 항상 정확한 비율을 유지하도록 함께 맞춘다(2026-09-16, 실제로 이 실수가 한 번 발생해서 재발 방지용으로 추가). 사용자가 이후 비율 변경을 요청하면, 비율을 바꾸지 않기로 이미 정했다는 것을 먼저 알린다

## 커뮤니티 사이드바 탭 강조/펼침 규칙

- **`docs/sidebar-tab-consistency-rule.md`를 반드시 확인한다**
- 커뮤니티 사이드바 카테고리 탭(`CommunityPage.tsx`의 `CATEGORIES`) 중 하나의 강조(활성화)/펼침/접힘 동작을 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 모든 탭에 동일하게 적용**한다 (공통 함수 `isCategoryActive()`, `DEFAULT_COLLAPSED_CATS`만 수정하고 탭별로 분기 로직을 새로 만들지 않는다)
- 새 카테고리 탭을 추가하면 이 문서도 함께 업데이트한다

## 커뮤니티 리스트 아이템 동일 적용 규칙

- **`docs/community-list-item-consistency-rule.md`를 반드시 확인한다**
- 이 문서에 나열된 "동일 그룹"(공지/게시물/인기글을 목록 행으로 보여주는 화면들) 중 하나의 아이템 내용 구성(제목 크기/색상, 닉네임 색상 등)을 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 같은 그룹의 나머지 전부에도 동일하게 적용**한다
- 단, 카드 테두리 등 **틀(컨테이너 스타일)은 이 규칙 대상이 아니다** — 사용자가 명시적으로 지정할 때만 바꾼다
- 새로 리스트 화면을 추가하면 이 문서의 목록도 함께 업데이트한다

## 커뮤니티 뒤로가기 버튼 규칙

- **`docs/community-back-button-consistency-rule.md`를 반드시 확인한다** (관리자/파트너 등 다른 도메인은 범위 밖 — 커뮤니티 화면 전용 규칙)
- 커뮤니티 상세/작성 화면의 뒤로가기 버튼은 `router.back()` + `from` 쿼리 파라미터(라벨 텍스트 전용) 패턴을 그대로 재사용한다
- `/community/{postId}`, `/community/announcement/{id}`, `/community/game-announcement/{id}`로 가는 링크를 새로 추가하면 반드시 정확한 `from` 라벨을 넘긴다 — 기본값에만 의존하지 않는다
- 새 커뮤니티 상세 화면이나 진입 링크를 추가하면 이 문서도 함께 업데이트한다

## 배지(뱃지) 재사용 규칙

- **`docs/badge-consistency-rule.md`를 반드시 확인한다**
- 같은 의미(상태/유형/카테고리 등)의 배지가 여러 화면에 쓰인다면 반드시 공용 컴포넌트 하나로 구현한다 — 화면마다 색상 클래스를 따로 하드코딩하지 않는다
- 공용 배지 컴포넌트 하나를 수정하면 **묻지 않고** 같은 배지를 쓰는 나머지 화면 전부에 자동 반영된다 (공용 컴포넌트 구조상 저절로 보장됨)
- 새 배지를 추가할 때 이미 같은 개념의 배지가 있는지 먼저 확인하고, 있으면 재사용한다. 이 문서의 배지 목록도 함께 업데이트한다

## 별점(평점) 디자인 통일 규칙

- **`docs/rating-consistency-rule.md`를 반드시 확인한다**
- 게임 평점을 별로 표시하는 곳은 전부 공용 컴포넌트 `apps/web/src/components/StarRating.tsx` 하나로 구현한다 — 화면마다 별 아이콘/색상을 따로 하드코딩하지 않는다
- 공용 컴포넌트 하나를 수정하면 **묻지 않고** 같은 컴포넌트를 쓰는 나머지 화면 전부에 자동 반영된다 (공용 컴포넌트 구조상 저절로 보장됨)
- 새로 평점(별점) 표시 화면을 추가할 때 이 문서의 목록도 함께 업데이트한다

## 날짜 표기 통일 규칙

- **`docs/date-format-consistency-rule.md`를 반드시 확인한다**
- 시간 없이 순수 날짜만 보여주는 곳은 전부 `apps/web/src/lib/formatDate.ts`의 공용 `formatDate()` 하나만 쓴다 (`2026. 8. 14.` 형식) — 화면마다 `getFullYear()` 조합이나 `toLocaleDateString()` 옵션을 따로 하드코딩하지 않는다
- 날짜 표기 하나를 수정하면 **묻지 않고** 같은 종류의 날짜를 쓰는 나머지 화면 전부에도 동일하게 적용한다
- 날짜+시간 결합 표시, 압축 마감 배지, 차트 축 라벨 등은 통일 대상이 아닌 예외 — 문서의 예외 목록 참고

## UI 디자인 기본 규칙

- **사용자가 따로 요청하지 않는 한, UI에 그림자(shadow) 효과를 절대 넣지 않는다.** 버튼, 카드, 뱃지, 아바타 등 어디에도 `shadow-*` 클래스를 임의로 추가하지 않는다 — 사용자가 명시적으로 "그림자 넣어줘"라고 요청한 경우에만 예외.
- 기존에 이미 들어있던 그림자 효과를 발견해도 먼저 물어보지 않고 임의로 제거하지는 않는다 — 이 규칙은 "새로 추가하지 않는다"는 뜻이지, 기존 걸 다 지우라는 뜻은 아니다. 다만 사용자가 "그림자 빼"라고 하면 즉시 제거한다.

## `<p>` 태그 폰트 크기 함정 (2026-09-17 발견 — 필수 확인)

- `apps/web/src/app/globals.css`에 **`p { font-size: 1rem; line-height: 1.6; }`** 전역 규칙이 있다. 이것 때문에 **부모 요소(`<td>`, `<div>` 등)에 `text-sm`/`text-xs`를 줘도, 자식 `<p>` 태그 자체에 크기 클래스가 없으면 상속되지 않고 무조건 16px(`1rem`)로 강제된다.**
- 실제로 이 문제 때문에 관리자/개발자 결제 테이블에서 같은 `text-sm`을 부모에 줬는데도 어떤 칸은 14px, 어떤 칸은 16px로 나오는 버그가 발생했다 — `<p>` 자체에 직접 크기 클래스가 있었는지(정상) 없었는지(16px로 깨짐)가 원인이었다.
- **`<p>` 태그를 쓸 때는 항상 그 `<p>` 태그 자체에 직접 폰트 크기 클래스(`text-sm` 등)를 명시한다.** 부모에만 걸고 자식 `<p>`가 상속받을 거라고 가정하지 않는다. (`<span>`, `<div>` 등 다른 태그는 이 전역 규칙 대상이 아니라 정상 상속됨 — `<p>` 태그만 이 문제가 있다)
- 새로 테이블/카드 등에 여러 줄 텍스트를 `<p>` 여러 개로 쌓을 때마다 이 함정을 체크할 것.

## 필수 입력 표시(*) 금지 규칙

- 입력 필드 라벨 뒤에 필수 표시용 `*`를 붙이지 않는다 (예: `게임 제목 *` → `게임 제목`)
- 게임관리 탭 전체에서 이미 일괄 제거함 — 새로 입력 필드를 추가할 때도 `*`를 넣지 않는다

## 관리자 상세정보 화면 내부 통계 카드 테두리 통일 규칙

- **`docs/admin-detail-stat-card-consistency-rule.md`를 반드시 확인한다**
- 관리자 상세정보류 화면에서 카드 내부에 다시 넣는 작은 통계 타일은 바깥 카드와 동일하게 `border border-line`을 쓴다 — 조회 전용 필드(수정 불가)는 타일이 아니라 Field+Input(disabled) 행으로, 수정 가능한 필드만 통계 타일로 넣는다
- 하나를 수정하면 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 같은 그룹의 나머지 화면 전부에도 동일하게 적용**한다
- 새로 이런 형태의 상세정보 화면을 추가하면 이 문서의 목록도 함께 업데이트한다

## 계정관리 회원 목록 테이블 버튼 동일 적용 규칙

- **`docs/admin-member-table-button-consistency-rule.md`를 반드시 확인한다**
- 계정관리 하위 회원 목록 테이블(기업회원/게임회원/관리자 등)의 "보기" 버튼 스타일 중 하나를 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 나머지 테이블 전부에도 동일하게 적용**한다
- 새 회원 목록 테이블을 추가하면 이 문서의 목록도 함께 업데이트한다

## 검색 입력창 디자인 규칙

- **`docs/search-input-consistency-rule.md`를 반드시 확인한다**
- **새로 검색 입력창을 만들 때는 무조건 이 문서의 표준 디자인을 그대로 쓴다** — 사용자가 특정 화면을 지목해 다르게 하라고 하지 않는 한 예외 없음
- 기존 화면들은 아직 일괄 전환 안 됨 — 사용자가 지목하는 화면부터 순차 전환
- placeholder에는 실제로 백엔드에서 검색되는 필드만 나열한다

## "회원" 대신 "회원님" 사용 규칙

- **`docs/member-honorific-consistency-rule.md`를 반드시 확인한다**
- 회원 개인을 직접 지칭하거나 회원에게 전달/안내하는 문구를 쓸 때는 무조건 "회원" 대신 "회원님"을 쓴다 (예: "회원에게 전달" → "회원님에게 전달")
- 단, 화면 제목/목록 등 구조적 명칭("회원 관리", "게임회원 상세정보" 등)은 대상 아님 — 문서의 예외 목록 참고
- 발견하는 대로 순차 적용하고 문서 목록도 갱신한다

## 입력 필드 라벨 동일 적용 규칙

- **`docs/field-label-consistency-rule.md`를 반드시 확인한다**
- 같은 화면(폼) 안의 입력 필드 라벨들은 폰트 크기·굵기·색상·여백 등 스타일이 전부 동일해야 한다
- 라벨 스타일 하나를 수정하면, 사용자가 "이건 이쪽만 다르게 해줘"라고 명시적으로 지정하지 않는 한 **묻지 않고 같은 화면의 나머지 라벨 전부에도 동일하게 적용**한다
- 새로 입력 필드를 추가할 때도 이 문서에 기록된 현재 스타일을 그대로 따른다

## 마이페이지 카드 제목 동일 적용 규칙

- **`docs/mypage-card-title-consistency-rule.md`를 반드시 확인한다**
- 마이페이지(플레이어/기업/관리자) 탭 안 카드 제목은 폰트(`text-lg font-semibold`), 아이콘 없음, 제목 아래 구분선(`border-b`)까지 전부 동일해야 하며, 카드 제목 텍스트는 그 카드가 속한 탭 이름과 반드시 일치해야 한다
- 하나를 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 문서에 나열된 나머지 카드 전부에도 동일하게 적용**한다
- 새로 마이페이지 카드를 추가하면 이 문서의 목록도 함께 업데이트한다

## 게임 상세 페이지 카드 제목 동일 적용 규칙

- **`docs/game-detail-card-title-consistency-rule.md`를 반드시 확인한다**
- `PlayerGameDetailPage.tsx`의 카드 제목(게임 설명/개발사 Q&A/이용 등급/개발사 만나기/별점 분포)은 전부 `text-text-primary font-semibold`, 18px로 동일해야 한다
- 하나를 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 문서에 나열된 나머지 카드 전부에도 동일하게 적용**한다
- 새로 이 페이지에 카드를 추가하면 이 스펙을 그대로 적용하고, 이 문서의 목록도 함께 업데이트한다

## 게임 상세 페이지 카드 폭·위치·간격 동일 적용 규칙 (베타존)

- **`docs/game-detail-card-layout-consistency-rule.md`를 반드시 확인한다**
- `PlayerGameDetailPage.tsx` 베타존의 좌측 그룹(개발사 정보/게임 설명)과 우측 그룹(평점/다른 베타 신청하기/별점 분포)은 폭·위치·간격이 하나의 그룹으로 취급된다 — 상단 블록과 하단 리뷰 블록 양쪽에 동일한 basis 비율(`53.7%` / 스페이서 `0.34%` / `22.32%`)이 적용돼 있어야 한다
- 하나를 수정하면, 사용자가 명시적으로 다르게 하라고 하지 않는 한 **묻지 않고 문서에 나열된 나머지 값 전부에도 동일하게 적용**한다
- 라이브존은 이 규칙 대상이 아님(공용 그리드 그대로 사용)

## 쿠폰 서버 (gameup-platform과 무관한 외부 미니 프로젝트)

- `deploy/coupon-server/` — 같은 운영 서버에 격리된 별도 Docker 컨테이너로 떠 있는 쿠폰 발급 서버. gameup-platform 본 서비스(웹/API/DB)와 코드·데이터 전혀 무관
- **제거/롤백 방법은 `docs/coupon-server-deployment.md`에 정리되어 있음 — 관련 작업 전 반드시 확인**
- **사용자가 "쿠폰 페이지 접근한 사람/IP 보여줘" 등으로 요청하면 `docs/coupon-server-visitor-check.md` 절차대로 확인해서 보여준다** (자동 차단 금지, 확인/보고만)
